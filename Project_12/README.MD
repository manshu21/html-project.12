Compound Interest Calculator (JavaScript)

Project Overview
This is a simple JavaScript project that calculates _Compound Interest_ and the _Total Amount_ after a given number of years.
The calculation is done using the standard compound interest formula.

Technologies Used

1.HTML
2.CSS
3.JavaScript

Project Structure
project-folder
│
├── index.html
├── style.css
├── script.js
└── image.png

Formula Used

Compound Interest Formula:

A = P (1 + r / (100 × n))^(n × t)

Where:

_P_ = Principal Amount
_r_ = Rate of Interest
_n_ = Number of times interest is compounded per year
_t_ = Time (in years)
_A_ = Total Amount

Compound Interest:

CI = A − P

JavaScript Code

let P = 5000;
let r = 10;
let n = 1;
let t = 3;

let A = P _ (1 + r / (100 _ n)) \*_ (n _ t);
let CI = A - P;

console.log("The total Amount after 3 years is", A);
console.log("The Compound Interest after 3 years is", CI);

```

How to Run the Project

1.Download or clone the project.
2.Open `index.html` in your browser.
3.Open the *browser console* to see the calculated result.

## Example

Input:

* Principal = 5000
* Rate = 10%
* Time = 3 years
* Compounded = 1 time per year

Output:

*Total Amount:** 6655
*Compound Interest:** 1655


```
